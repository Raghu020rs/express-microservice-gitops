apiVersion: v1
kind: Service
metadata:
  name: express-app-service
  namespace: default
  labels:
    app.kubernetes.io/name: express-app
spec:
  type: ClusterIP
  selector:
    app: express-app
  ports:
  - name: http
    port: 80
    targetPort: 3000